[project]
name = "kairos"
version = "0.1.0"
description = "Privacy-first AI assistant that understands your workday."
readme = "README.md"
requires-python = ">=3.12"

authors = [
    { name = "Thomas Hallberg" }
]

dependencies = [
    "sqlalchemy",
    "pywin32",
    "psutil"
]